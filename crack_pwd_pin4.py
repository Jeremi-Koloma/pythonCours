from string import digits

for i in digits:
    for j in digits:
        for k in digits:
            for l in digits:
                print(i, j, k, l)

# 0 à 9 -> 10chiffres et 04 colonnes donc 10*4 -> 10 000 posibilités